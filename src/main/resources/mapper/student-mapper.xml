<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.school.dao.interfaces.IStudentsDAO">
    <resultMap id="StudentResult" type="com.solvd.school.model.Student">
        <id property="id" column="id"/>
        <result property="firstName" column="first_name"/>
        <result property="lastName" column="last_name"/>
        <result property="classId" column="class_id"/>
        <result property="groupId" column="group_id"/>
    </resultMap>

    <select id="getById" parameterType="int" resultMap="StudentResult">
        SELECT id, first_name, last_name, class_id, group_id FROM students WHERE id = #{id}
    </select>

    <select id="getByClassId" parameterType="int" resultMap="StudentResult">
        SELECT id, first_name, last_name, class_id, group_id FROM students WHERE class_id = #{classId}
    </select>

    <select id="getByGroupId" parameterType="int" resultMap="StudentResult">
        SELECT id, first_name, last_name, class_id, group_id FROM students WHERE group_id = #{groupId}
    </select>

    <select id="getAll" resultMap="StudentResult">
        SELECT id, first_name, last_name, class_id, group_id FROM students ORDER BY id
    </select>

    <insert id="insert" parameterType="com.solvd.school.model.Student" useGeneratedKeys="true" keyProperty="id"
            keyColumn="id">
        INSERT INTO students (first_name, last_name, class_id, group_id)
        VALUES (#{firstName}, #{lastName}, #{classId}, #{groupId})
    </insert>

    <update id="update" parameterType="com.solvd.school.model.Student">
        UPDATE students
        SET first_name = #{firstName},
        last_name = #{lastName},
        class_id = #{classId},
        group_id = #{groupId}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM students WHERE id = #{id}
    </delete>
</mapper>
