<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.school.dao.interfaces.IGeneticAlgorithmParamsDAO">
    <resultMap id="GAPResult" type="com.solvd.school.model.GeneticAlgorithmParams">
        <id property="id" column="id"/>
        <result property="maxGenerations" column="max_generations"/>
        <result property="allowedGaps" column="allowed_gaps"/>
        <result property="populationSize" column="population_size"/>
        <result property="mutationRate" column="mutation_rate"/>
        <result property="crossoverRate" column="crossover_rate"/>
        <result property="description" column="description"/>
        <result property="isActive" column="is_active"/>
    </resultMap>

    <select id="getActive" resultMap="GAPResult">
        SELECT id, max_generations, allowed_gaps, population_size, mutation_rate, crossover_rate, description, is_active
        FROM genetic_algorithm_params WHERE is_active = TRUE ORDER BY id DESC LIMIT 1
    </select>

    <insert id="insert" parameterType="com.solvd.school.model.GeneticAlgorithmParams" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
        INSERT INTO genetic_algorithm_params
        (max_generations, allowed_gaps, population_size, mutation_rate, crossover_rate, description, is_active)
        VALUES (#{maxGenerations}, #{allowedGaps}, #{populationSize}, #{mutationRate}, #{crossoverRate}, #{description},
        #{isActive})
    </insert>

    <update id="update" parameterType="com.solvd.school.model.GeneticAlgorithmParams">
        UPDATE genetic_algorithm_params
        SET max_generations = #{maxGenerations},
        allowed_gaps = #{allowedGaps},
        population_size = #{populationSize},
        mutation_rate = #{mutationRate},
        crossover_rate = #{crossoverRate},
        description = #{description},
        is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM genetic_algorithm_params WHERE id = #{id}
    </delete>
</mapper>
