# Database configuration
db.url="jdbc:mysql://localhost:3306/school_schedule?useSSL=false&serverTimezone=UTC"
db.username="root"
db.password="1518"
db.driver=com.mysql.cj.jdbc.Driver

# Connection pool settings
db.pool.size=10
db.connection.timeout=30
db.idle.timeout=10
db.max.lifetime=1800

# SSL configuration
db.ssl.mode=DISABLED